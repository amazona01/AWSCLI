#creacion de instancia Ubuntu con grupo de seguridad mensajeria

resource "aws_instance" "ubuntu_server" {
  ami                    = "ami-04b4f1a9cf54c11d0"  # Ubuntu Server 20.04 en us-east-1
  instance_type          = "t2.micro"
  subnet_id              = aws_subnet.public1.id
  key_name               = aws_key_pair.ssh_key.key_name
  associate_public_ip_address = true
  vpc_security_group_ids = [aws_vpc.main.id] # Asigna el grupo de seguridad CMS

  tags = {
    Name = "Ubuntu-Server-${var.nombre_alumno}"
  }
}
